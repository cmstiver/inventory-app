extends layout

block content
  h1= title
  
  ul
  each sandwich in sandwich_list
    li 
      a(href=sandwich.url) #{sandwich.name} 
      | (#{sandwich.price})

  else
    li There are no sandwiches.
